#ifndef POLYGON_SHUFFLE_ARRAY_H
#define POLYGON_SHUFFLE_ARRAY_H

// Объявим Функцию для перетасовки массива
void shuffleArray(int* &array, int &size);
// Объявим функцию для тестирования
bool testShuffle(int expectedSize, const int* expectedArray);
// Объявим функцию для запусков самих тестов
bool runTests();


#endif //POLYGON_SHUFFLE_ARRAY_H
